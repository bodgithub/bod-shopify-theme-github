---
category: Product
---

Trust strip card. Grid of icon + title + sub-label trust cells inside the signature blue gradient border frame (or the solid navy card). Use 4 or 8 items; icons: leaf, flask, shield, star, capsule, drop, usa, heart.

```tsx
<TrustStrip headline="Why women choose Bodology" items={[{icon:"flask", title:"3rd-Party Tested", sub:"COA Published"}, ...]} />
```
