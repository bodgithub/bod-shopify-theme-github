{%- liquid
	if customer
  	assign profile_link = routes.account_url
	else
  	assign profile_link = routes.account_login_url
	endif
-%}
<div class="thb-secondary-area thb-header-right">
  
  <!-- Hide Quick Search by adding in style=display:none -->
  
	<a class="thb-secondary-area-item thb-quick-search" href="{{ routes.search_url }}" style="display:none;" title="{{ 'sections.header.search' | t }}">{%- render 'svg-icons' with 'search' -%}</a>


  
	{%- if shop.customer_accounts_enabled -%}

      		<!-- Replace Account Icon with Text by adding { comment } tags -->

    <a class="thb-secondary-area-item thb-secondary-myaccount" href="{{ profile_link | escape }}" title="{{ 'sections.header.my_account' | t }}"> {% comment %}{%- render 'svg-icons' with 'myaccount' -%}{% endcomment %}</a>
  {%- endif -%}


    	<!-- Conditionally show Cart only when an item is added by adding line 25, 28 and { comment } tags -->
  	{%- if cart.item_count > 0 -%}
  
	<a class="thb-secondary-area-item thb-secondary-cart" href="{{ routes.cart_url }}" id="cart-drawer-toggle">cart {% comment %}<div class="thb-secondary-item-icon"> {% render 'svg-icons' with 'cart' %}{% endcomment %} <span class="thb-item-count">{%- if cart.item_count > 99 -%}{%- render 'svg-icons' with 'thb-asterisk' -%}{%- else -%}{{ cart.item_count }}{%- endif -%}</span></div></a>
  	{%- endif -%}

      <!-- Add the Button to the Right by adding all lines from 31-32 -->
  	<a class="button" href="https://bodology.co/products/myo-d-chiro-inositol-powder" role="button" title="Get inositol">
    	<span>Get inositol</span>
  	</a>
      
      </div>